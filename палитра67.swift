import SwiftUI

// MARK: - 11. ЕДИНАЯ ЦВЕТОВАЯ ПАЛИТРА (Premium Neon)
extension Color {
   // Основной фон (Очень глубокий сине-черный)
   static let premiumBackground = Color(red: 0.05, green: 0.05, blue: 0.08)
   
   // Неоновые светящиеся акценты (Dribbble Style)
   static let neonBlue = Color(red: 0.20, green: 0.60, blue: 1.0) // Электрический синий
   static let neonPurple = Color(red: 0.60, green: 0.20, blue: 1.0) // Кибер-фиолетовый
   static let neonOrange = Color(red: 1.0, green: 0.40, blue: 0.10) // Огненный оранжевый
   static let neonGreen = Color(red: 0.10, green: 0.85, blue: 0.40) // Токсичный зеленый
   static let neonRed = Color(red: 1.0, green: 0.15, blue: 0.30) // Яркий красный
   static let neonYellow = Color(red: 1.0, green: 0.85, blue: 0.10)
}
